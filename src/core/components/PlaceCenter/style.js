import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  container: {
    display: "flex",
    placeContent: "center",
    placeItems: "center",
    width: "100%",
    height: "100vh"
  }
}));
